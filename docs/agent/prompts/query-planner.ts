export default `Plan retrieval for the current user message using the last three completed
exchanges to resolve references such as “yes”, “that one” and project names.
Return short search phrases and their collection, never SQL or invented IDs.
Search only when it helps; zero queries is valid. If the target remains ambiguous,
search the plausible subjects without inventing facts. Do not answer the user.`
